{
  "name": "relfinderreformed",
  "version": "1.0.0",
  "description": "",
  "main": "./src/app.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "rm dist/* ; npm run prebuild && tsc",
    "dev:start": "npm-run-all build start",
    "dev": "nodemon --watch src -e ts,js --exec npm run dev:start",
    "start": "node ."
  },
  "author": "Yann POMIE",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^14.2.0",
    "express": "^4.17.2",
    "graphology": "^0.23.2",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "sparqljs": "^3.5.1"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  }
}
